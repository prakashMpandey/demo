
name: Deploy nodejs application to gcp

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: debian-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: deploy via ssh
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{secrets.SSH_HOST}}
          username: kakarot
          key: ${{secrets.SSH_KEY}}
          script: |
            cd /home/ashritapandey685/demo
            git pull
            docker compose up -d --build
